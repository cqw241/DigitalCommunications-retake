# 数字通信原理 7 天冲刺：Day 5 学习进度

> 目标：35 小时内最大化期末达到 60–70 分的概率。本文件记录 Day 5 已完成并通过练习/小测的内容，作为 Day 6–Day 7 的学习交接。

## 总体状态

- ✅ Day 5：第 11 章差错控制编码高收益部分
- ✅ Day 5 模块小测已通过

---

# Day 5：差错控制编码

## ✅ 1. 码重（Hamming weight）

二进制码字的码重等于其中 1 的个数。

\[
w(c)=\text{码字中非零元素的个数}
\]

例如：

\[
w(10100110)=4
\]

已能稳定完成码重计算。

---

## ✅ 2. 汉明距离与最小码距

两个等长二进制码字的汉明距离等于对应位置不同的位数：

\[
d(c_1,c_2)=w(c_1\oplus c_2)
\]

若其中一个码字为全零码：

\[
d(0,c)=w(c)
\]

最小汉明距离：

\[
d_{\min}=\min_{c_i\ne c_j}d(c_i,c_j)
\]

对于线性分组码，可直接使用：

\[
d_{\min}=\text{所有非零码字中的最小码重}
\]

---

## ✅ 3. \((n,k)\) 码与码率

对于 \((n,k)\) 线性分组码：

- \(k\)：信息位数
- \(n\)：编码后总码长
- \(n-k\)：校验位 / 监督位 / 冗余位数

码率：

\[
R=\frac{k}{n}
\]

例如 \((15,11)\) 码：

\[
R=\frac{11}{15},\qquad n-k=4
\]

---

## ✅ 4. 最小码距与检错 / 纠错能力

若最小码距为 \(d_{\min}\)：

最多保证检测：

\[
\boxed{e_{\max}=d_{\min}-1}
\]

最多保证纠正：

\[
\boxed{t_{\max}=\left\lfloor\frac{d_{\min}-1}{2}\right\rfloor}
\]

反向判断：

若要求保证检测 \(e\) 位错误：

\[
d_{\min}\ge e+1
\]

若要求保证纠正 \(t\) 位错误：

\[
d_{\min}\ge 2t+1
\]

曾在小测中把检错 / 纠错公式混淆，但补考已纠正：当 \(d_{\min}=5\) 时，能够正确得到“检 4 位、纠 2 位”。

---

## ✅ 5. 用生成矩阵 \(G\) 编码

核心公式：

\[
\boxed{c=uG}
\]

所有运算均为模 2 运算，加法等价于 XOR。

应试技巧：信息向量 \(u\) 中某一位为 1，就选取 \(G\) 的对应行；为 0，则该行不参与。最后将所有选中的行做 XOR。

例如对 \((7,4)\) 码，若：

\[
u=(1,0,1,1)
\]

则选择 \(G\) 的第 1、3、4 行做 XOR。

曾在小测中把 \((7,4)\) 编码结果只写成 4 位信息位，没有写完整 7 位码字；补考中已能正确写出完整码字，例如：

\[
c=1011010
\]

---

## ✅ 6. 系统形式生成矩阵 \(G=[I_k\ P]\)

若生成矩阵为：

\[
\boxed{G=[I_k\ P]}
\]

其中 \(I_k\) 为 \(k\times k\) 单位矩阵，则：

\[
c=uG=u[I_k\ P]=[u\mid uP]
\]

因此在这种常见系统形式中，码字前 \(k\) 位会直接保留原信息位。

已理解：

- 这不是所有生成矩阵都具备的性质；
- 若 \(G\) 不是系统形式，则码字前 \(k\) 位不一定等于原信息；
- 也可能存在 \(G=[P\ I_k]\) 一类系统形式，此时信息位位于后 \(k\) 位。

生成矩阵尺寸：

\[
G:k\times n
\]

例如 \((7,4)\) 码：

\[
G:4\times7
\]

---

## ✅ 7. 由 \(G\) 求校验矩阵 \(H\)

当：

\[
G=[I_k\ P]
\]

二进制线性分组码可写：

\[
\boxed{H=[P^T\ I_{n-k}]}
\]

其中方括号表示横向拼接，不是矩阵乘法。

尺寸关系：

\[
H:(n-k)\times n
\]

例如 \((7,4)\) 码：

\[
H:3\times7,\qquad H^T:7\times3
\]

并理解校验矩阵满足：

\[
GH^T=0
\]

---

## ✅ 8. 伴随式 / syndrome 校验

对于接收码字 \(r\)：

\[
\boxed{s=rH^T}
\]

若：

\[
s=0
\]

则通过校验；若：

\[
s\ne0
\]

则检测到错误。

快速计算方法：找出 \(r\) 中为 1 的位置，把 \(H\) 对应列做 XOR。

---

## ✅ 9. 单比特错误的伴随式定位与纠错

在只发生 1 bit 错误的前提下：

\[
\boxed{\text{syndrome }s=\text{H 中出错位置对应的那一列}}
\]

因此译码步骤为：

1. 计算 \(s=rH^T\)
2. 在 \(H\) 中找与 \(s\) 相同的列
3. 该列编号即错误位
4. 将该位 \(0\leftrightarrow1\) 翻转完成纠错

训练中曾主动发现题目给出的 syndrome 与 \(r,H\) 不一致，并重新计算得到正确结果，说明该部分已经能够独立验算而非机械套用。

---

## ✅ 10. FEC / ARQ / HARQ 基本区别

### FEC（Forward Error Correction）

发送端加入纠错冗余，接收端尽量自行纠错，一般不依赖反馈重传。

关键词：**直接纠错、无反馈重传**。

### ARQ（Automatic Repeat reQuest）

接收端检测到错误后，通过反馈要求发送端重传。

关键词：**检错 + 反馈 + 重传**。

### HARQ（Hybrid ARQ）

结合 FEC 与 ARQ：先尝试纠错，纠错不足时再请求重传。

关键词：**纠错 + 必要时重传**。

按照当前冲刺策略，不展开 Stop-and-Wait、Go-Back-N、Selective Repeat 的窗口与协议计算。

---

# Day 5 小测结果

Day 5 模块小测覆盖：

- 码重
- 汉明距离
- 最小码距
- 码率
- 检错 / 纠错能力
- \(uG\) 编码
- \(G\to H\)
- syndrome 校验
- 单比特错误定位 / 纠错
- FEC / ARQ / HARQ

最终状态：**✅ 通过**。

小测暴露出的两个问题：

1. 曾混淆 \(d_{\min}\) 对应的检错 / 纠错公式；
2. 曾在 \((7,4)\) 编码题中只写 4 位信息位而不是完整 7 位码字。

两项均已通过补考纠正。

---

# 当前学习进度

- ✅ Day 1：基础信息论、信道与噪声
- ✅ Day 2：第 6 章数字基带传输
- ✅ Day 3：第 7 章二进制数字带通调制
- ✅ Day 4：第 10 章抽样、量化与 PCM / A 律 13 折线
- ✅ Day 5：第 11 章差错控制编码核心部分

## 下一步

Day 6 按原计划进入：

**第 5 章模拟调制最低限度 + 第 2/3 章最低限度 + 混合题训练。**

原则继续保持：只补高收益基础分，不钻复杂推导。
